[project]
name = "trade-sentinel"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["fastapi>=0.115", "uvicorn[standard]>=0.30", "sqlalchemy>=2.0", "aiosqlite>=0.20", "httpx>=0.27", "pandas>=2.2", "numpy>=2.0", "apscheduler>=3.10", "pydantic-settings>=2.5"]

[tool.uv]
package = false

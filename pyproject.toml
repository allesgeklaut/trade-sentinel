[project]
name = "trade-sentinel"
version = "0.3.0"
requires-python = ">=3.13"
dependencies = ["fastapi>=0.115", "uvicorn[standard]>=0.30", "sqlalchemy>=2.0", "aiosqlite>=0.20", "httpx>=0.27", "pandas>=2.2", "numpy>=2.0", "pydantic-settings>=2.5", "yfinance>=0.2.54", "scipy"]

[tool.uv]
package = false

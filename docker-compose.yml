services:
  trade-sentinel:
    build: .
    container_name: trade-sentinel
    restart: unless-stopped
    ports: ["8002:8000"]
    env_file: .env
    volumes: ["trade_sentinel_data:/data"]
    security_opt: ["no-new-privileges:true"]
    cap_drop: [ALL]
    read_only: true
    tmpfs: ["/tmp"]
    environment:
      - TZ=Europe/Vienna
volumes:
  trade_sentinel_data:
